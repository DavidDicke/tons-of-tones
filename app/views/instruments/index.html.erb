<h1 class="mt-5 text-center">Instruments</h1>

<div class="container mt-4">
  <div class="row">
    <% @instruments.each do |instrument| %>
      <div class="col-md-4 mb-4">
        <%= render partial: "shared/instrument_card", locals: { instrument: instrument } %>
      </div>
    <% end %>
  </div>
</div>
